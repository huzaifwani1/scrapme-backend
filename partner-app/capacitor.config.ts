import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.scrapme.partner',
  appName: 'ScrapMe Partner',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
